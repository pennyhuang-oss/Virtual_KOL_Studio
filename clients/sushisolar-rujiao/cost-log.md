# 成本紀錄（流水帳）

> 用途：累積真實數據，供日後對外報價參考。**不是成本模型**，見 `CAMPAIGN_PLAN.md` 9.2。

| 日期 | 項目 | 模型 | credits | 重製次數 | 備註 |
|---|---|---|---|---|---|
| 2026-08-26 | 批次 1 pilot v1：YG-06 ×2、LG-05 ×2 | `soul_2` + soul_id，2K | **0.48** | 0 | 光線公式驗證批 |
| 2026-08-27 | 批次 1 pilot v2：YG-06 ×1、LG-05 ×1 | `soul_2` + soul_id，2K | **0.24** | — | 表情具名寫法驗證批 |
| 2026-08-27 | 控制變因測試 R1：C 組、D 組各 ×1 | `soul_2` + Yuna soul_id，2K | **0.24** | — | `transactions` 逐筆確認 −0.12 ×2；見 `CALIBRATION_TEST.md` 第 10 節 |
| 2026-08-27 | 追加測試 P：逆光根因（只改背景一句） | `soul_2` + Yuna soul_id，2K | **0.12** | — | 見 `CALIBRATION_TEST.md` 第 11 節 |
| 2026-08-27 | 室內驗證：咖啡廳窗邊、浴室鏡前各 ×1 | `soul_2` + Yuna soul_id，2K | **0.24** | — | 同時段有一筆非本專案的 Seedance 2.5 −117，勿計入；見第 12 節 |
| 2026-08-27 | Luna soul_id 測試：甜點店門口 ×1 | `soul_2` + Luna soul_id，2K | **0.12** | — | 見 `CALIBRATION_TEST.md` 第 13 節 |

> ⚠️ **對帳方法更正（2026-08-27）**：先前一度用「餘額差額」反推成本，得出「單張 33 credits」的錯誤結論。
> 實際查 `transactions` 後確認：**兩張 Soul V2 各 0.12，共 0.24**；
> 同期少掉的 66 credits 是同帳號其他工具（Voice Element ×2 = 80、Seed Audio ×8、GPT Image 2.0）所致，與本專案無關。
> **正確做法：事前用 `get_cost: true` 預檢，事後查 `transactions` 逐筆對帳——不要看餘額差額。**

## 已知的單價（持續更新）

| 項目 | 實測單價 | 對照 pricing 文件的估值 |
|---|---|---|
| 圖（`soul_2` + soul_id，1536×2048） | **≈0.12 credits/張** | 4 credits/張 → **高估約 33 倍** |
| 影片（`kling3_0` 單鏡頭 10–15s） | 待測 | 304 credits（那是 40–60 秒多段口播，非同類） |

> ⚠️ **圖片成本幾乎可以忽略。**批次 1 原估「圖 40 張 ≈160 credits」，
> 實際大約只要 **5 credits**。這代表**多生幾張選一張的成本壓力極低**，
> 往後每個場景可以生 3–4 張變體（不同 prompt 措辭）而不是 2 張，
> 提高一次到位的機率。真正的成本在影片。

---

## 2026-08-26 生成方式變更：一個 spec 只生一張

pilot 用 `count=2` 跑了兩組，**每組兩張的構圖、姿勢、服裝幾乎一模一樣**。
第二張沒有提供新資訊，等於把同一個結果再看一次。

→ 往後 **一個 spec = 一張**。不滿意就改 prompt 再生。
要同場景多張變體，寫成不同的 spec（不同表情、不同景別），不靠 `count`。

**這不是為了省成本**（圖只要 0.12 credits/張），是為了**每一次生成都要學到東西**。
