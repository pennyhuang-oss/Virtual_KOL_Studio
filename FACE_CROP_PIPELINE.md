# 部件裁切管線（crop_spec v1）

## 為什麼要有這條管線

2026-08-29 的四格消融證明：**裁切是必要因素，措辭不是。**

| 組 | 輸入 | 指派措辭 | 部件有沒有被執行 |
|---|---|---|---|
| A | 四張完整人臉 | 短版 | ❌ |
| B | 部件裁切 | 強版 | ✅ |
| C | 部件裁切 | 短版 | ✅ |
| D | 四張完整人臉 | 強版 | ❌ |

D 組的措辭已經寫到 `Ignore every unassigned facial feature in each image`，
模型連「忽略」這個指令都忽略。這與本 repo 最老的那條規則同源：
**這個模型不執行否定句。要移除的東西必須在輸入端移除，不能靠文字叫它忽略。**
裁切就是「在輸入端移除」的實作。

## 組成

| 檔案 | 作用 |
|---|---|
| `pilot/crop_spec.json` | 版本化規格：各槽的長寬比、輸出尺寸、landmark 錨點、邊距、padding、QA 門檻 |
| `tools/build_face_crops.py` | 依 landmark ＋ 規則 deterministic 產生裁切，寫 manifest |
| `tools/check_face_crops.py` | 驗證 manifest 與雜湊，並執行「prompt 只能引用通過 QA 的 crop」這道 gate |
| `tools/gen_crop_qa_sheet.py` | 產生人工 QA 印樣 |
| `pilot/face_crops_manifest.json` | 每一件的 provenance 與 QA 結果 |
| `review/batch3_face_crops/<version>/` | 產出的裁切檔 |

**唯一鍵是 `(source_ref_id, slot, crop_spec_version)`。** 同一來源同一槽只裁一次、跨 persona 重用，
所以工作量是「被使用的 unique source-slot 組合」，不是 19×4＝76 次。目前分配是 51 個組合。

## 四個槽位

| 槽位 | 長寬比 | 輸出 | 取景 | 達成長寬比的方式 |
|---|---|---|---|---|
| FACE_SHAPE_AND_JAW | 4:5 | 1024×1280 | 髮際上方留白到下巴以下，含雙耳與顎角 | 擴張取景（這一槽需要臉周圍的脈絡） |
| EYES_AND_BROWS | 3:1 | 1536×512 | 眉上緣到下眼眶下方，**含雙眼雙眉**，不含鼻頭 | 補中性灰邊 |
| NOSE | 1:1 | 768×768 | 鼻根到鼻小柱下緣，左右各超出半個鼻翼寬 | 補中性灰邊 |
| MOUTH | 2:1 | 1024×512 | 人中上緣到頦唇溝，左右各超出四分之一口寬 | 補中性灰邊 |

**絕不拉伸。** 三個部件槽用補灰邊而不是擴張取景——擴張會讓 1:1 的鼻框吃進眼睛、
2:1 的嘴框吃進鼻尖，那就違背了裁切的目的。

## QA 門檻

| 檢查 | 門檻 | 為什麼 |
|---|---|---|
| padding_ratio | ≤ 0.12 | 部件框越出原圖邊界的比例。太多表示部位被原圖截斷。為長寬比補的灰邊不計入。 |
| yaw_proxy | ≤ 0.14（雙眼裁切 ≤ 0.08）| 鼻尖到左右臉緣的水平距離差 ÷ 臉寬。眼距是獨立的 identity 軸，側身會讓兩眼透視不等、眼距失真。 |
| 原圖部位短邊 | ≥ 96px | 低於這個數字，放大到規定尺寸只會得到糊圖。 |
| bleed | 逐槽 | 眼框不得含鼻尖；嘴框不得含鼻尖；鼻框不得含完整的眼睛或上唇。 |

`check_face_crops.py` 會擋下任何引用到未通過 QA 之 crop 的角色。

## 2026-08-29 的實測結果

**原本 15 張真人照片，作為裁切來源基本上不能用。**

```
目前分配的 51 個 (來源, 槽位) 組合   → 37 個 QA 未過
其中來自舊 15 張的 44 個            → 37 個未過（只有 7 個過）
其中來自新 8 張的 7 個              → 7 個全過
新 8 張跑滿四槽的 32 個             → 32 個全過
因此被擋住的角色                    → 19/19
```

原因不是修圖，是**拍攝條件**：那 15 張是社群上的生活照與活動照，多數是四分之三側身
（yaw_proxy 0.16–0.68，而標準正面是 0.00–0.02），臉在畫面裡也小，
嘴與鼻在原圖上只有三、四十個像素。裁切法對來源的要求，比整張臉當參考時嚴格得多。

**容量試算**（依 ChatGPT 的 cap：臉型 ≤2 位、其餘 ≤3 位；且排除 low）：

| 槽位 | 可用來源 | 容量 | 需求 |
|---|---|---|---|
| FACE_SHAPE_AND_JAW | 10（新 8 ＋ ref_01、ref_04）| 20 | 19 **只多 1，幾乎沒有餘裕** |
| EYES_AND_BROWS | 8（全部是新圖）| 24 | 19 |
| NOSE | 8 | 24 | 19 |
| MOUTH | 8 | 24 | 19 |

也就是說：**整個管線實際上會全部跑在合成的標準正面來源上，舊的 15 張真人照片自然退出。**
這順帶把「合成臉可能與某個真實存在的人相似」這個風險移除了——
合成來源有完整的 provenance 與逐檔雜湊（`review/batch3_face_refs/SOURCES.json`）。

臉型槽 20 對 19 的餘裕太薄：只要有任何一張的骨架方向與某個角色的原型不合，就沒有替代品。
建議再補幾張標準正面來源當臉型池的緩衝。
