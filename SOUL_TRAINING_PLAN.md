# Soul 訓練規劃（19 位）

**狀態：規劃中，尚未送出任何生成。** 待 ChatGPT 覆核 + 使用者拍板後才執行。

---

## 0. 這一步之前已經定案的東西（不要重做）

- **19 位的 identity master 全部完成**，鎖在 `kols/<id>/identity/identity_master.jpg`。
- 臉部來源規則、prompt 字典、瀏海規則、真人辨識度規則，都在 `BATCH3_FACE_INVARIANT.md`。
- 舊的 `MODELING_SHOOT_PLAN.md` Phase A（4 個候選 identity）與 Phase B（校準）
  **已被這一輪取代**——當時要解決的問題就是「先確定這個人長什麼樣」，現在已經有了。
  **本規劃從 Phase C（20 張訓練圖）開始。**

---

## 1. 為什麼不能直接把 identity master 送去訓練

訓練 endpoint 要 5–20 張。只有 1 張 master，所以要先長出 20 張。

**長的方式決定成敗，這件事 repo 裡已經有血的教訓：**

> **2026-07-25 Vicky Lin 事故**（`SEXY_SCENE_LIBRARY.md` §454）：
> 用同一組文字 prompt 各自獨立呼叫 8 次，**得到的是 8 個不同的人**——
> 風格一致，但不是同一個人的 8 個角度。把這種圖送進 Soul 訓練，
> 學到的是多人特徵的平均，不是一個穩定身分。

> **rainie-hsu v1 事故**：錨點只核對臉與妝、沒核對身材，
> 訓練出來的身型與 94-59-92／F 的設定不符，13 張訓練圖與該 soul 全部作廢重做。

所以 20 張必須**錨定在同一個身分上**，而且錨點要同時管住**臉和身材**。

---

## 2. 機制：Reference Element（已對照工具 schema 確認，不是憑記憶）

`show_reference_elements(action='create')`：

- 吃 `medias[]`（**複數**，可以放多張圖），同步回傳 `element_id`。
- 用法是在 prompt 裡嵌 `<<<element_id>>>`，後端直接把該圖注入生成。
- **支援 `seedream_v4_5`**——正是我們一路在用的模型。
- **不支援 Soul V2。** Element 是鷹架，Soul 是成品，兩者不衝突。

`show_characters(action='train')`：

- `name` + **5–20 張**參考圖，約 10 分鐘，非阻塞。
- 訓練完的 soul 只能配 `model: soul_2` 或 `soul_cinematic`。

---

## 3. 每位的五個步驟

| 步驟 | 做什麼 | Credits |
|---|---|---|
| **C-0 身材錨點** | identity master 只有頭肩，**沒有身體**。先生成 2 張全身／及膝、身材可讀的候選，使用者選 1 張。 | 2 |
| **C-1 建 Element** | 把 master（臉）+ C-0 選定圖（身材）**兩張一起**餵給 `show_reference_elements(action='create')`，得到 `element_id`。 | 未知，見 §6 |
| **C-2 生 20 張訓練圖** | 每張 prompt 嵌 `<<<element_id>>>`，**只變**景別／角度／表情／姿勢／場景／穿搭／髮型。 | 20 |
| **C-3 訓練前 QA** | 用 §4 的配額與硬退件表逐張檢查。**沒過就不送訓練**——訓練是這條線上最貴的一步。 | 0 |
| **C-4 Soul 訓練** | `show_characters(action='train')`，20 張。 | 約 25（估計，見 §6） |
| **C-5 壓力測試** | 用 `soul_2` + soul_id 跑 Phase D，照 `soul_qa_rubric` 評分。 | 6–8 |

**單人合計：約 53–55 credits。**

---

## 4. 20 張要涵蓋什麼（沿用 `pilot/nico_pilot.json` 的 `phase_c_quota`，已驗證過的規格）

**結構**：8 張乾淨身分錨點 + 12 張生活場景 = 20。

| 維度 | 配額 |
|---|---|
| 頭部角度 | 正面 ≥3、左 30° ≥2、右 30° ≥2、左 60° ≥1、右 60° ≥1、側臉 ≥1 |
| 景別 | 臉部特寫 ≥2、胸上 ≥4、腰上 ≥3、全身 ≥4 |
| 其他 | 姿勢相異 ≥4、表情相異 ≥5、臉部無遮擋 ≥10、服裝相異 ≥6 |
| 視角 | 他拍 ≥9、自拍 ≤4 |
| 濾鏡 | 無濾鏡 ≥11、CCD ≤2、美圖 0 |
| 乾淨錨點下限 | 臉部特寫乾淨 2、全身身材可讀 3、右側乾淨 1 |
| 光線 | 單一難看頂光 ≤3（困難光線留給 Phase D，不進訓練集） |
| 場景集中度 | 整體 ≤0.4、生活場景子集 ≤0.55 |
| 列順序 | **每位各自 shuffle**，任何欄位都不得與列號綁定 |

**訓練集專屬例外（`SEXY_SCENE_LIBRARY.md` §285）**：公共場景**只有她一個人**，不寫背景路人——
第二個人可能被學進身分。代價是訓練集看起來比日常素材假（空景的超商、空景的月台）。
**這是刻意的取捨；訓練完成後日常素材必須立刻切回「有背景路人」的寫法。**

**髮型髮色**：Soul V2 **不繼承**訓練圖的髮型髮色，所以訓練集裡可以多變化，
但之後每次生成都必須在 prompt 裡重寫。

---

## 5. 訓練前的硬退件表（C-3）

任一張中就退，重生那一張：

1. 臉被遮住、雙眉不可見
2. 兩眼不聚焦、瞳孔位置左右不一致
3. 五官接縫、虹膜破損
4. 畫面中出現第二個人或第二隻不屬於她的手
5. 年齡讀起來偏離設定 ±4 歲以上
6. 身材與 `profile.json` 的三圍／罩杯明顯不符（**rainie v1 就是死在這一條**）
7. 與該位的 identity master 並排看不像同一個人

第 7 條是這一輪學到的：**每一張都要跟 master 並排比對**，不是只看單張好不好。

---

## 6. 我不確定、需要先確認的事

| # | 問題 | 現況 |
|---|---|---|
| **U-1** | `show_reference_elements(action='create')` **收不收費？** | 工具說明沒寫。repo 沒有紀錄。**先做 1 位，看 `transactions` 對帳。** |
| **U-2** | Soul 訓練實際多少 credits？ | repo 只有「依 Rainie Hsu 案例觀察約 25」，並註明**未逐筆核對**。同樣靠第一位對帳確認。 |
| **U-3** | Element 放兩張（臉 + 全身）會不會反而讓模型混淆？ | 沒測過。schema 允許多張，但沒說多張如何合成。**這是 §8 要問 ChatGPT 的第一題。** |
| **U-4** | 20 張都嵌同一個 element，會不會把 element 那張的**背景／服裝**也一起烙進去？ | 沒測過。 |

---

## 7. 成本：先把數字攤開

單人約 **53–55 credits**。

| 範圍 | Credits |
|---|---|
| 1 位（pilot，含對帳） | ~55 |
| 19 位 | **~1,010–1,045** |
| 目前餘額 | **1,348** |

**更正一件事**：我上一則說「Soul 訓練 19 位共 475 credits」——**那個數字漏掉了 380 credits 的訓練圖生成**，
以及身材錨點與壓力測試。實際約是兩倍。餘額仍然夠，但沒有第二次全部重來的空間。

---

## 8. 建議的執行順序

**先做 1 位 Vertical Slice，跑完 C-0 到 C-5，拿到一個評分過的 soul，再決定要不要放大。**

理由不是保守，是這一輪臉部建模的教訓：一次性大批送出，錯了就是整批重來。
1 位是 55 credits，19 位是 1,010——**先花 5% 買掉 U-1 到 U-4 四個未知數。**

**pilot 人選建議：`wanyin-jiang`**
- 黑色及腰長直髮、無特殊染色 → 髮型不會干擾判讀
- 身材 D 罩杯、非極端 → 身材錨點好驗
- 她的 master 是這批最正面、最乾淨的其中一張

（若要挑最難的當 pilot，則是 `kanon-komori`：粉紫漸層 + 20 歲 + 及腰長髮，
髮色與年齡都容易漂。兩種選法我沒有強烈偏好，看你要先驗「順利路徑」還是「最壞情況」。）

---

## 9. 待 ChatGPT 覆核的問題

見 `review/REVIEW_SOUL_TRAINING_R1.md`。
