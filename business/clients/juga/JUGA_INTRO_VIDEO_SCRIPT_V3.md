# JUGA 介紹片 × Rainie Hsu — 腳本 V3（定案文案）

> **這份文件取代 `HANDOFF_TO_AIGC_PRODUCER.md` 第 3 節的「方向 A / 方向 B」判斷。**
> 舊文件是在「這是一支操作教學片」的假設下寫的；2026-08-26 客戶端資訊更新後，題目換了，
> 不再是教學片。舊文件（`JUGA_APP_GUIDE_SCRIPT_V2.md`、`juga-rainie-script.html`、
> `juga-rainie-aigc-worksheet.html`）**仍然有效的部分**：Rainie 造型規格、三條紅線、
> soul_id、生成 prompt 組裝方式、交片檢核清單——這些跟「腳本內容是教學還是介紹」無關，
> 繼續適用，本文件不重複貼。**已經失效的部分**：18 步驟教學結構、57 句操作型旁白、
> 「加到主畫面」段落、方向 A/B 的舞蹈段安排邏輯——這些是綁在教學片假設上的，本文件取代。

**交接日**：2026-08-26（Vincent 接手當天，同一輪對話內完成方向定案）
**Repo**：`pennyhuang-oss/Virtual_KOL_Studio`
**目錄**：`business/clients/juga/`

---

## 1. 為什麼題目換了

客戶端 2026-08-26 給出三點更新，推翻了舊交接包的教學片假設：

1. **JUGA 現在還是個網站，不是 App**——「加到主畫面」那 20 秒的教學段落不再需要
2. **影片要做的是「介紹它在幹嘛」，不是「介紹怎麼操作」**——舊文件的 18 步驟操作教學結構整個不適用，
   57 句操作型旁白（「點這裡」「滑到這裡」）也不適用
3. **我們可以自己註冊、實際操作網站**——不再被綁死在客戶原本那支 65.7 秒截圖接成的原片上，
   舊交接包第 2-2 節那四個瑕疵的後製硬解方案（假狀態列、區域裁切、標題裁切、分頁不進入）
   也連帶失去存在理由——如果要拍新畫面，直接拍乾淨的就好，不需要繞這些後製

## 2. 定位判斷：這支片不能讓 Rainie 演「主導篩選對象」

**核心限制**：JUGA 的產品設計是給男生主動約女生用的。Rainie 是女性 KOL，如果腳本把她寫成
「我發邀請、我篩選誰能加入」（例如「你挑人，不是人挑你」這類句子），示範出來的互動方向
跟產品設計的方向是反的——等於教觀眾一個跟產品實際使用情境相反的行為模式。

討論過三個修正方向：

| 方向 | 做法 | 結論 |
|---|---|---|
| A | Rainie 演「被邀請的一方」，故事翻過來 | 更有代入感，但需要新拍「加入別人的邀請」這條流程的畫面，目前沒有這段畫面的實測紀錄 |
| B | Rainie 退回中性導覽/主持人，不進故事 | ✅ **採用**，可沿用既有素材邏輯，且不需要新驗證任何流程 |
| C | 情境換成不帶篩選意味的邀約（揪姊妹去 After Party） | 治標不治本——故事結構沒變，她依然是發起+篩選的一方，只是換了比較不尷尬的說法 |

**選定方向 B**，後續演化成 V3：Rainie 以主持人身分直接對觀眾喊話介紹產品，不進入故事、
不扮演發起者或被邀者任何一方，因此完全不觸及「誰主導、誰篩選」的性別角色問題。

## 3. 腳本演化記錄

### V1 — 廣告感開場（情境排比）

「你」對觀眾喊話，四句情境排比（找人吃飯／喝一杯／補位／After Party）堆疊出痛點，
轉場後正式介紹產品、講解機制、品牌 tagline 收尾。**保留供參考**，未採用——語感偏「唸稿」，
不夠短影音。

### V2 — Storytime 版（第一人稱敘事）

Rainie 用第一人稱講自己上禮拜五的一件事，產品是「順便提到」，不是正式介紹。跟 V1 的差異：
人稱從「你」換成「我」、開場從排比情境換成具體的一次事件、產品出現方式從標語登場改成故事帶出來。
**保留供參考**，未採用——這版把她寫成故事裡發起邀請、篩選對象的角色，觸及第 2 節的定位問題。

### V3 — 定案版（中性主持人 + 直接對男性觀眾喊話）

在方向 B 的基礎上，把開場對象明確鎖定「各位哥哥」，情境痛點從「找對象」改成「找朋友陪」，
機制說明段落沿用 B 版但拿掉了跟前段重複的分類清單、拿掉了操作教學語感的「發一則邀請」句式。

逐句修訂記錄（供之後回溯為什麼這樣寫）：

1. 開場情境原本用「翻開通訊錄」，改成「訊息傳了一輪，已讀不回」——通訊錄是過時的動作，
   現在沒人靠翻通訊錄找人
2. 開場對象從中性的「你」改成「各位哥哥」，情境從「想找對象」改成「想找朋友吃飯/喝一杯/唱歌」，
   先講朋友、後面才揭曉「網站上有優質女孩陪你」——這個轉折是刻意的，比一開始就講女孩更像鋪梗
3. 「已讀不回」單一講法改成「要嘛已讀不回，要嘛沒消沒息」，涵蓋兩種真實情境
4. 「優質妹妹」改成「優質女孩」（用詞修正）
5. 機制段落原本寫「發一則邀請，喝一杯、找人補位、臨時約會、After Party，都行——限時兩小時」，
   問題有二：(a) 跟前一句「想吃飯、想喝酒、想唱歌」講的是同一件事（活動類型）但用了不同詞，
   聽起來像兩份不同清單；(b) 「發一則邀請」這個祈使句式又掉回教學片的操作語感。
   改成「隨時發一則邀請，不分白天晚上，24 小時都約得到人」，拿掉重複的分類清單，
   賣點從「限時緊迫感」換成「隨時都方便」
6. 「有人想加入，會自己舉手」的「舉手」比喻換成「進入聊天視窗」，同時把後面
   「配對成功，開一個只活一天的聊天視窗」合併進來，簡化成「想加入的人，自己會進入聊天視窗」——
   這個簡化**跳過了原本「邀請人審核、接受/婉拒」這個中間步驟**，是刻意的（介紹片不需要交代這麼細），
   但代表原本產品機制裡「你可以挑」這個環節在旁白裡不見了，如果之後要靠畫面/字卡補回來，
   這是要注意的點
7. 「打開聊天視窗」用詞順序微調（走進→進入）

## 4. 定案文案（V3）

```
各位哥哥，是不是常常這樣——
想找朋友吃飯、喝一杯、唱歌，訊息傳出去，要嘛已讀不回，要嘛沒消沒息。
揪來揪去，就是揪不到人陪。

今天跟你們介紹一個網站——JUGA。
上面很多優質女孩，想吃飯、想喝酒、想唱歌，通通找得到人陪你。

隨時發一則邀請，不分白天晚上，24 小時都約得到人。
想加入的人，自己會進入聊天視窗——喬好地點時間，就能出發。
不用早早約好。今晚才決定，也來得及。
今晚有局，因為有你——JUGA。
```

抓速念完約 45–50 秒。

## 5. 還沒決定的事

| # | 待決事項 | 說明 |
|---|---|---|
| 1 | **長度** | 目前 45–50 秒，客戶預設「一分多鐘，最多這個上限」——偏短，要不要加內容撐長還沒定 |
| 2 | ~~Rainie 出鏡比重與方式~~ | ✅ **已定案**：她會出鏡且**開口對嘴講話**（不是純旁白），造型沿用 R10 黑框眼鏡版。分段與畫面分配見第 9 節 |
| 3 | **畫面來源** | 要不要現在就去註冊實測網站，拍乾淨的新畫面，還是仍以客戶原片為底 |
| 4 | **「限時兩小時」「只活一天」兩個數字** | V3 定案文案裡都已經拿掉，賣點改成「24 小時都約得到人」的隨時方便性。若後製想在畫面/字卡上補回這兩個數字（例如倒數計時視覺），旁白目前沒有對應的口語鋪陳 |
| 5 | **「你可以挑」的篩選環節** | V3 把「發起人審核、接受/婉拒」這個中間步驟簡化掉了，只保留「發邀請→有人進聊天室」，是否要在畫面（而非旁白）補回這個環節 |

## 6. Rainie 造型/紅線/生成規格 — 仍以舊文件為準

以下內容跟教學片/介紹片的腳本方向無關，繼續適用，不重複貼：

- **三條紅線**（腰腹包覆、禁用詞觸發拼貼、眼鏡要明寫）→ `HANDOFF_TO_AIGC_PRODUCER.md` 第 1 節
- **Soul ID、造型參考圖、現成舞蹈素材**→ `HANDOFF_TO_AIGC_PRODUCER.md` 第 2 節
- **生成 prompt 組裝方式、共用參數**→ `juga-rainie-aigc-worksheet.html` 第 01、03、04 節
- **交片檢核清單**→ `juga-rainie-aigc-worksheet.html` 第 08 節

## 7. 起始畫面：R10 v1 修圖定案

**背景**：Vincent 決定這支介紹片的起始畫面直接沿用 `kols/rainie-hsu/images/dance_clone_r10/start_frame.png`
（v1，扶眼鏡手勢），不用已核准的 `start_frame_v2.png`（拿手機自拍手勢）——即使 v1 有第 1 節紅線提到的
腰間鏤空問題。**裁決**：先試 v1，之後拿去生成如果因為腰間問題被判 `nsfw`，屆時再處理，不預先修腰間。

v1 原圖另外還有兩個瑕疵，2026-08-27～28 用 `seedream_v5_pro`（`is_inpaint: true`）修復：

1. **手部旁邊有一圈不明的白色發光弧線**——後來確認是背景的環形補光燈，因為構圖角度貼著手部，讀起來像
   憑空冒出的光源瑕疵。已修掉，背景改用窗簾/暗處自然接續
2. **臉部角度太側，眼神沒有看鏡頭**——已修成臉朝鏡頭多轉一些，眼神直視

**修圖過程踩的坑（供之後修圖參考）**：
- 每次疊代編輯（拿上一版結果當底繼續修）都會讓色溫/白平衡跟著漂移，跑了 5、6 輪都對不準 Vincent
  指定的目標色溫（就是 v1 原圖本身的暖黃+藍窗對比）。最後靠的是**從最原始的 v1 重新做一次修圖**
  （不疊在前面幾版編輯結果上），把「手部白光」跟「臉部角度」兩個修正一次做完，同時把「色溫必須跟
  來源完全一致」寫成明確的強制條件，才拿到一個 Vincent 覺得「還可以接受」的版本
- `seedream_v5_pro` 的「校色」不是精準可控的操作——同一個目標，連續兩次嘗試一次更接近、一次反而更
  偏離，沒有穩定的方向性。**如果之後還要再修色溫，建議直接交給人工後製（Lightroom / DaVinci Resolve）
  用滴管工具校正，不要再賭生成模型**
- 呼叫 `generate_image` 時一定要明確帶 `aspect_ratio` 參數對齊來源圖的比例（本案來源是 9:16）——
  沒帶的話模型會用預設的 1:1 正方形輸出，構圖跟來源完全對不上，浪費一次生成

**產出檔案**：`business/clients/juga/assets/start_frame_fixed.png`——Vincent 核准的最終版（白光已修、
臉朝鏡頭、色溫「還可以接受」）。**這是修過的檔案，不是原始 v1**，原始 v1（`start_frame.png`）保留在
`kols/rainie-hsu/images/dance_clone_r10/` 不動，供對照與未來重修使用。

## 8. 配音：音色已核准，固定 voice_id 卡在帳號額度，改用即時 audio_references

**背景**：`HANDOFF_TO_AIGC_PRODUCER.md` 第 5 節把配音路線交給接手的製作師決定，列了幾條查證過但
未實測的路。2026-08-27 實際測試如下：

1. **soul_id 起始畫面當 `image_references` 線索**（`seed_audio` + Rainie 的 `start_frame_v2.png`）：
   生成多組，測試「台灣腔＋御姐系」方向。`pitch_rate`／`speech_rate` 只能調音高/語速，
   調不出口音本身；使用者回饋降 pitch 會讓音調變怪，改回原始音高、只調語速
2. **Vincent 提供的 ElevenLabs 參考音檔**（`Tiffy — Taiwanese Bilingual Narrator`）：改用
   `audio_references` 直接克隆這份音檔的音色，套用我們自己寫的文字（避開參考音檔本身的錯字咬字）
3. **Vincent 提供的第二份參考音檔**：同樣走 `audio_references` 克隆，套用完整 V3 定案文案
   （43.7 秒單次生成，非逐句拼接），**Vincent 核准此音色**

**下一步卡住的地方**：依計畫要送 `create_voice_from_confirmed_audio` 把這個音色固定成
`voice_id`，回傳 `Error creating voice: Voice limit reached — delete a voice to add a new one.`——
帳號的自建（element）語音數量已達上限。現有 3 個：`Faye Tan`、`Tan XiaoXiao`、`Yulenda Clean`（見
`HANDOFF_TO_AIGC_PRODUCER.md` 第 5 節，Penny 聽過覺得都不像 Rainie 的人設，但不確定是否有其他
KOL/專案仍在使用，刪除語音不可逆，未經確認不能自行刪除)。

**Vincent 裁決（2026-08-27）**：**不解決額度問題，暫不建立固定 `voice_id`**。正式生成時直接把
參考音檔當 `audio_references` 現場餵給 `seed_audio`，不透過固定聲音資產。

**這個做法的已知風險**：沒有固定 `voice_id`，每次呼叫 `seed_audio` 都是重新從參考音檔「抽一次」
音色，不是鎖定同一個訓練好的聲音模型——多次獨立呼叫之間音色/語氣有可能有些微飄移。**因應方式**：
正式生成時盡量比照 2026-08-27 的測試做法，一次餵完整段文字生成，不要切成多支獨立呼叫；如果之後要
做長版（例如回頭套用舊版 57 句/12 段旁白），切分點越多、音色跑掉的風險越高，屆時需要重新考慮解決
`voice_id` 額度問題（刪除現有 3 個之一，或請客戶/Penny 加開額度）。

**參考音檔存放**：使用者上傳的兩份參考音檔（ElevenLabs Tiffy 樣本、第二份核准樣本）目前只存在
Higgsfield 平台的 media 資產裡（`media_id` 見對話紀錄），未存入本 repo——如需在其他 session 重現
這個聲音，需要 Vincent 重新提供音檔。

## 9. 分鏡：她入鏡 vs 網站畫面的分配

**原則**：頭尾她在、中間換網站畫面。比例約六四分（她六成、網站四成）。

| # | 段落 | 台詞 | 畫面 | 秒數 |
|---|---|---|---|---|
| 1 | 開場情境 | 各位哥哥，是不是常常這樣——想找朋友吃飯、喝一杯、唱歌，訊息傳出去，要嘛已讀不回，要嘛沒消沒息。揪來揪去，就是揪不到人陪。 | **她入鏡** | ~15 |
| 2 | 產品揭曉 | 今天跟你們介紹一個網站——JUGA。上面很多優質女孩，想吃飯、想喝酒、想唱歌，通通找得到人陪你。 | 她起頭，講到「優質女孩」時**切進網站畫面** | ~10 |
| 3 | 機制說明 | 隨時發一則邀請，不分白天晚上，24 小時都約得到人。想加入的人，自己會進入聊天視窗——喬好地點時間，就能出發。 | **網站畫面**為主 | ~15–18 |
| 4 | 收尾 CTA | 不用早早約好。今晚才決定，也來得及。今晚有局，因為有你——JUGA。 | **回到她** | ~5 |

**為什麼這樣分**：
- 段 1 是搏共鳴，需要看到表情，畫面給她
- 段 3 資訊量最大，實際操作畫面比她站著講有說服力
- 段 4 回到她收尾，品牌 tagline 由人講比較有力

### 對嘴需求帶來的三個硬限制（2026-08-28 確立）

這支片是**對嘴片**（她會開口講話，不是純旁白），所以切點不能像純旁白那樣自由：

1. **每支 clip 上限 15 秒**——所有支援對嘴的影片模型（`seedance_2_0`、`wan2_7`、`grok_video_v15`、`seedance_2_0_mini`）都被 15 秒封頂。全片 45–50 秒必須切成 4 段個別生成，不可能一鏡到底
2. **切點必須落在句子/語氣停頓的邊界**——中途切走再切回來，嘴型跟聲音會對不上。上表的四段分界就是照語意段落切的，不要再細分
3. **每段都要固定首尾幀**——維持造型/場景連續性，段與段之間才接得起來

### 網站畫面怎麼疊上去

段 2、段 3 換成網站畫面時，**她的對嘴影片仍然要完整生成**，網站畫面是後製疊上去蓋掉畫面、聲音繼續走。這樣做的好處是：如果剪輯覺得某段網站畫面不夠好看，可以隨時改回她的畫面，不用重新生成。

**尚未決定**：網站畫面的來源（自己註冊實測錄影 vs 沿用客戶原片）——見第 5 節待決事項 3。
