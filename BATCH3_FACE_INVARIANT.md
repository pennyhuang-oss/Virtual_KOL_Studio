# Batch 3 臉部素材的不可協商規則

**這一條由使用者訂下，任何規劃、任何覆核意見、任何量測結果都不能推翻它。**

---

## 規則

> **剩下 19 個人設的臉，一律用 `ref_01`–`ref_15` 這 15 張美女參考圖的五官 remix 而成。**

配套的三條，同樣不可協商：

1. **這 19 個人必須是漂亮的美女。** 「好看」是第一驗收條件，排在骨相正確、
   分離度、可量測性之前。任何 gate 只要沒有把「好看」列進去，那個 gate 就是錯的。
2. **`ref_16`–`ref_58` 永久退出。** 那是我在走歪期間請 ChatGPT 生的替代素材
   （素顏證件照模板與極端幾何版），檔案保留作失敗紀錄，但**不得再進入任何分配**。
3. **不得再用另生一批參考圖來「解決」素材不足。** 素材不夠時的解法是
   放寬門檻、提高重用上限、或對側臉美女做中介照，**不是換掉那 15 個人**。

---

## 為什麼要把它寫成檔案

這條規則原本只存在於對話裡。中途對話被壓縮過一次，我把「裁切管線」帶了下來，
卻沒把「素材就是那 15 張」帶下來，於是後面每一層都建立在錯的素材上。

**寫進倉庫、並由 `tools/check_batch3_face_invariant.py` 自動檢查，
是為了讓這條規則在我失憶之後仍然生效。**

---

## 走歪的完整經過（留著，不要刪）

1. 我寫了裁切 QA，門檻是正面度與部件像素——**量的是「好不好裁」，
   沒有任何一項在量「好不好看」**。
2. 那 15 張生活照 44 格只過 7 格，我因此判定「不適合當裁切來源」，
   請 ChatGPT 生標準正面模板取代 → `ref_16`–`ref_33`，**素顏證件照素人**。
3. 素人來源做出來的 19 個人彼此太像，我寫了優化器
   **最大化「最接近那一對的距離」**，窗開到極值 → `ref_40`–`ref_58`，
   使用者的評語是「外星人般的醜女」。
4. 我還讓每張參考圖同時扛四個不同人物的目標，
   **等於要求每張圖本身就是四個不相干的人拼起來的臉**。
5. 另外，我的成像措辭字典整套在把人拍醜（可見毛孔、膚色不均、禁止美化、
   全景深、背景雜亂、允許死白、素顏），所以就算不掛裁切也生不出美女。

**每一步單獨看都「通過了檢查」，因為檢查裡沒有美感這一項。**

---

## 檢查

`python3 tools/check_batch3_face_invariant.py`

它會在下列情況失敗：

- 任何 persona 的四個臉部槽位指向 `ref_01`–`ref_15` 以外的來源；
- `ref_16`–`ref_58` 出現在任何 active 分配裡；
- donor-slot 表或分配表把 `ref_16`+ 當成可用來源。

---

## 追加規則：衝突時以來源為主（使用者裁定）

> **人設的文字描述與參考圖來源衝突時，一律以來源為主。**

理由（使用者原話的重點）：**照片真實、有依據、五官漂亮；人設只是文字，
每次送出去生成不一定完全照文字走。** 實際在控制輸出的是那四張裁切圖，不是形容詞。

執行上的三個後果：

1. 19 位的臉部依據改成「實際指定的四張來源的判讀」，存在 `face_from_source`。
   原本 ChatGPT 規劃的 11 條文字軸值移到 `_axes_superseded_by_source` 留檔，**不再作為依據**。
2. 生成 prompt 裡**不再放骨相形容詞**（長卵形、窄顎那類）。四張裁切圖負責形狀，
   文字只負責年齡、族裔、髮色、妝、光線、構圖這些裁切圖管不到的東西。
3. 之後若再出現「來源跟人設對不上」，**不是問題，不用回報**——直接以來源為準，
   並把該位的 `face_from_source` 更新成新來源的判讀。

人設的個性、職業、身材數據、髮色、年齡、族裔**完全不動**，那些不是臉部特徵。

---

## 追加規則：瀏海與「建檔照 ≠ 公開造型」（使用者提問後訂下）

> **identity master 是建檔照，不是她的公開造型。建檔照必須露眉；
> 造型（瀏海、髮色、髮長）是套在已鎖定臉上的一層，日常內容想怎麼弄都行。**

### 為什麼建檔照要露眉

不是「有瀏海就訓練不起來」。真正的兩個理由：

1. **驗不到。** 這條管線的整個重點是「四張裁切圖各自負責一個部位，而且真的有被執行」。
   眉毛被蓋住時，`EYES_AND_BROWS` 那一格到底有沒有落地，**沒有辦法確認**——
   看不到的東西不能當作通過。
2. **眉型會變成未定義。** identity master 是之後 20 張 Soul 訓練圖與所有日常內容的依據。
   建檔照沒有眉，往後每一次生成都在自己編一副眉毛，同一個人前後不一致。

另外，Soul 訓練若整組都是厚瀏海，模型會把「瀏海」學成身分的一部分，
之後想把瀏海撥開反而做不到。

### 想要瀏海的人設怎麼處理

| 階段 | 規則 |
|---|---|
| identity master | **一定露眉。**可用「露眉的瀏海」：空氣瀏海（透光、眉毛看得穿）、八字／法式瀏海（中分、眉露出）、瀏海撥兩側。 |
| Soul 訓練 20 張 | 大多數露眉（定義臉），其中留 4–6 張她的招牌厚瀏海（讓模型學會這個造型），**不要整組都是厚瀏海**。 |
| 日常內容 | **完全自由。**厚瀏海、齊瀏海、狗啃瀏海都可以，臉已經鎖定了。 |

所以「這個人設就是要有瀏海」和「建檔照要露眉」不衝突——
差在哪一張照片，不是差在這個人能不能有瀏海。

---

## 追加規則：臉型槽是身分主導槽（由退件資料反推）

> **兩個人設不得共用同一張 `FACE_SHAPE_AND_JAW` 來源。**
> **`ref_15` 永久退出臉型槽**（它當臉型時會把本人的辨識度整個帶過去）。

### 資料

使用者在第三波退了 7 位，其中 5 位的理由是「跟某某太像」。把這幾對拿去比四格：

| 這一對 | 共用的格 |
|---|---|
| rin-ayase vs cheryl-soh | 只有臉型（都是 ref_02） |
| yerin-han vs tammy-chou | 只有臉型（都是 ref_10） |
| wendy-yeo vs zhiyi-shen | 只有臉型（都是 ref_11） |
| emma-kao vs kanon-komori（都被說像原圖本人） | 只有臉型（都是 ref_15） |

**四對「太像」全部只共用臉型槽，其他三格完全不同。**
反過來，共用眼／鼻／口的組合沒有任何一對被說像。

### 原本錯在哪

舊約束是「任兩位共用格數 ≤ 1」，**四個槽位一視同仁**。
實際上臉型槽決定輪廓、顎線、下巴——也就是人臉辨識最先看的東西；
眼鼻口只是填在裡面。臉型一樣，換眼鼻口救不回來。

### 現在的分配

臉型槽用滿 15 張裡的 14 張（`ref_15` 除外），19 位中 5 張需要服務 2 人。
共用臉型的那 5 對必須：其他三格完全不同、族裔不同或年齡差 ≥3、髮色系不同，
**而且在定案前要並排目視確認沒有撞臉**。

## 追加規則：老態與眼神

- **老態**：`angeline-kwee` 兩張都被退，理由是嘴邊紋路深、眼神老態。
  prompt 一律加正面描述（不是否定句）：臉頰平滑無法令紋、眼周無細紋、皮膚緊實。
  另外**不要笑**——微笑會把法令紋拉出來。
- **眼神**：`wanyin-jiang` 被退，理由是兩眼不聚焦、像視力有問題。
  prompt 一律要求：兩眼一起對準鏡頭、瞳孔在虹膜中的位置左右一致、
  兩眼反光點的位置一致。

---

## 追加規則：真人辨識度（第二次踩到，改成硬規則）

> **臉型槽用到「一眼認得出是哪個真人」的參考圖時，輸出就是那個真人。**
> 已否決：`ref_15`（第一次）、`ref_03`（第二次）。兩張都不得再進臉型槽。

- `ref_15` 當臉型 → emma-kao、kanon-komori 兩位都被認出是原圖本人。
- `ref_03` 當臉型 → wendy-yeo 被認出是高允貞，「等於照搬她的五官」。

眼／鼻／口槽沒有出現過這個問題——**只有臉型槽會把整個人帶過去**，
跟前一條「臉型是身分主導槽」是同一個機制。

### 執行上的一件事：這個判斷我做不到，只有使用者能做

我認不出參考圖裡的人是誰。每次都是生成完、使用者看到才發現。
所以**在送生成之前**，要先把「哪張參考圖被指派到誰的臉型槽」整理成一張圖給使用者看，
讓她把認得出的真人先否決掉。表在 `review/donor_cards/_face_sources_in_use.jpg`。

### 素材餘額

15 張裡扣掉 `ref_15`、`ref_03`，臉型槽只剩 **13 張**要分給 19 位，
所以至少 6 對必須共用臉型。共用的那幾對一律要並排目視確認。

---

## 診斷：為什麼臉型槽會複製真人（使用者指出後才查清楚）

使用者的話：「我認得出真人沒有用啊，是送出去的時候，它模型自己帶入了整張臉的
五官設定去生成。這是生成的問題啊。」——**對的，我原本提的「請你先否決認得出的人」
不是解法，只會把素材池越砍越小。**

### 實際原因

四個輸入根本不對稱：

| 輸入 | 實際內容 |
|---|---|
| Image 2 眼 | 1536×512 橫條，只有眼眉 |
| Image 3 鼻 | 768×768 小方塊，只有鼻 |
| Image 4 口 | 1024×512 橫條，只有嘴 |
| **Image 1 臉型** | **1024×1280，髮際到下巴、耳到耳、背景保留** |

三個是部件，**第四個是一張完整人像**。而 R5 J-01 的四格消融早就證明
「整張臉當參考時模型不執行部件指派」——那條結論一直在規格裡，
但沒人發現臉型槽自己就是一張整臉。模型拿到真人正面照，照著畫，
輸出就是那個真人，完全合理。

### 解法：留輪廓、抹五官（`tools/mask_face_crop.py`）

ChatGPT J-02.1 否決過純剪影，理由是會失去臉長寬、三庭與顎角——顧慮成立，
但它只考慮了全有或全無。中間解是**只把五官區域高斯模糊，
輪廓、頰緣、顎角、髮際、三庭原樣保留**。比對圖：
`review/donor_cards/_mask_test_sheet.jpg`（V1 模糊可用；V2 填平抹掉頰部立體感、
V3 連輪廓都糊掉，兩者皆不可用）。

**待驗證**：拿最糟的案例測——wendy-yeo 用遮蔽後的 `ref_03`（就是被認出高允貞那張）。
過了就全面換成遮蔽版，`ref_03` 與 `ref_15` 解禁回池，臉型來源恢復 15 張、
共用壓力下降；沒過就代表臉型槽不可用，要另想。
