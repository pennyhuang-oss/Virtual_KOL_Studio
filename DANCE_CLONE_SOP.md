# Dance Clone SOP — 熱門舞蹈複製流程（Method B：動作驅動法）

> 標準工作流程：丟一支 TikTok / Instagram 上的熱門跳舞短影音，抓它的音樂與舞步，
> 讓本 studio 的 KOL 重新演出這支舞。
>
> 本文件是 [`DANCE_VIDEO_SOP.md`](DANCE_VIDEO_SOP.md)（Method A：AI 自主編舞法）的**姊妹流程**，
> 不是取代它。兩者的差異、各自優劣、以及移植依據，見 [`DANCE_METHOD_COMPARISON.md`](DANCE_METHOD_COMPARISON.md)。

---

## 怎麼觸發：一句指令，Claude 在同一個 session 內做完全部步驟

使用者只需要下一個指令，例如：

> 「用 Iris Chen 重跳這支舞：`<TikTok 或 Instagram 連結>`」

Claude 收到這句話後，**在同一輪對話裡**依序執行 Step 0–8 的每一步（下載、裁剪、生成規格書、
呼叫 Motion Control、確認音軌、必要時混音、檢核），**不需要使用者手動用 CapCut 或其他剪輯軟體**。

> **⚠️ 2026-08-07 補充（人工核准關卡，之前只是默契、沒寫成規則）**：上面「一次做完全部步驟」指的是
> **不需要使用者手動操作剪輯軟體**，不是「Step 4 生成起始畫面後直接自動跑 Step 5 Motion Control」。
> **Step 4 起始畫面生成完成後必須停下來，把圖給使用者看，等使用者明確核准（或指示打回重生成）之後才能
> 進入 Step 5**——Motion Control 有實際 credit 成本，起始畫面如果服裝/場景/五官不對，讓使用者在跑
> Motion Control 之前就能攔下來重來，不要等生成完整支影片才發現要重做。R1、R4、R5、R8 實際執行時都是
> 這樣做（起始畫面核准或打回，見各 KOL `generation_notes.md` Step 4 章節），只是没有寫成文字規則，
> 現在補上，避免新的 session 或別人接手時漏掉這個關卡直接往下跑。

> **⚠️ 2026-08-05 更正**：本節原本寫「Kling 3.0 生成出來的影片本身是無聲的，一定要靠 Step 6 手動
> 混音」，這個假設沒有實測驗證就寫進文件，是錯的。實測（Coco Wu 案例）確認：**當驅動片本身含音軌時，
> `motion_control` 通常會自動輸出已經合成好、跟生成動作對時過的音樂**，不是無聲畫面。所以正確流程是
> Claude 先用 `ffprobe` 檢查 Step 5 的輸出有沒有音軌，**多數情況下可以直接把這個檔案當成品**，
> Step 6 的手動混音只在「輸出真的無聲」或「要換成已授權的曲庫版本」時才需要執行。

使用者收到的是**已經有背景音樂、可以直接看的成品**，不是無聲影片加一句「請自己去配樂」——這個結論
不變，只是「音樂從哪來」這一步的機制跟原本寫的不一樣。

---

## 這套方法解決什麼問題

Method A（音樂驅動、AI 自主編舞）的舞步是模型自己想的，**沒有「記憶點招牌動作」**，線條也不夠俐落。
Method B 直接借用一支已經爆紅、已經證明過「有記憶點、卡點乾淨」的真人舞蹈片當**動作驅動（driver）**，
讓 AI 只需要做「換臉換場景」，不需要自己編舞——編舞這件事，人類舞者已經幫忙驗證過了。

**兩套方法互補，依情境選用**：

| 情境 | 用哪套 |
|------|--------|
| 要跟當週熱曲、可自由換歌、要快速大量出片 | **Method A**（`DANCE_VIDEO_SOP.md`） |
| 有一支明確想複製的熱門舞蹈（副歌記憶點動作已經被驗證會紅） | **Method B（本文件）** |
| 旗艦片、重點帳號的主打內容 | **Method B**，慢工出細活 |

---

## 大量選片與分配 SOP（Sourcing at Scale，2026-08-05 新增，2026-08-05 改用 GitHub Issue）

> 目標：一次處理一批熱門舞蹈候選，經人工篩選後平均分配給 12 位 KOL，每支影片只讓一位 KOL 複製，
> 同一位 KOL 不重複拿到同一批裡已經指定給別人的影片。

> **⚠️ 2026-08-05 改版**：原本用 Google Sheet 追蹤，但 Drive 工具只能新建檔案、不能就地更新既有 Sheet，
> 每次收集新候選就要整份重建成新檔案，版本會越開越多，使用者還要自己清理——**改用單一個持續存在的
> GitHub Issue 取代 Sheet**：GitHub Issue 的 checklist 支援直接點擊勾選（不用像 Sheet 那樣自己套用
> 核取方塊格式），而且可以直接編輯 issue 內文附加新項目，不會產生多個檔案版本。

### 追蹤位置：GitHub Issue

**[`pennyhuang-oss/Virtual_KOL_Studio` Issue #3](https://github.com/pennyhuang-oss/Virtual_KOL_Studio/issues/3)**
——「🩰 舞蹈影片候選清單追蹤」，只用這一個 issue，不開新的。

Issue 內文分三個區塊：
- **`## 待篩選`**：checklist，每行格式 `` - [ ] `ID` | 連結 | 平台 | 來源類型 | 簡述 | 發現日期 ``，
  使用者在 GitHub 網頁上直接點擊勾選（打勾 = 核准）
- **`## 已分配`**：已經分配給某位 KOL 的項目會從「待篩選」移過來，格式
  `` - [x] `ID` | 連結 | 分配KOL | 生成狀態 ``，生成完成後把「待生成」改成實際結果（比照
  `kols/coco-wu/generation_notes.md` 的紀錄方式，連結過去）
- **`## 命名趨勢情報`**：沒有具體連結、只是「這類型/這首歌最近很紅」的參考情報，不是可核准的候選片，
  不用勾選

### 三步驟

1. **收集候選** —— 收集邏輯不變（見下方「候選來源可信度」），但寫入方式改成編輯 issue 內文：
   用 `mcp__github__issue_read(method='get', issue_number=3)` 抓目前內文，在「待篩選」區塊裡插入新的
   checklist 行（**檢查 ID 沒有跟既有項目重複**），用 `mcp__github__issue_write(method='update',
   issue_number=3, body=<合併後的完整內文>)` 寫回去——GitHub 沒有「附加」API，每次都要整份 body 重送，
   但這仍然是**同一個 issue**，不會多開檔案。
2. **人工篩選** —— 使用者直接在 GitHub 網頁打開這個 issue，點擊勾選想核准的項目（GitHub 會自動存檔，
   不需要另外通知任何人），篩完在 issue 底下留言或跟 Claude 說一聲即可
3. **分配給 KOL**：

```bash
# 1. 抓目前 issue 內文，存成本機檔案
#    （用 mcp__github__issue_read 取得 body，存到 current_body.md）

# 2. 解析出「已勾選 + 有具體連結」的項目，轉成 CSV
python3 tools/parse_dance_issue.py current_body.md > approved.csv

# 3. 分配給 KOL（邏輯不變）
python3 tools/assign_dance_batch.py approved.csv --seed <本次執行日期> > assignment.csv

# 4. 把分配結果套回 issue 內文：從「待篩選」移到「已分配」並標註 KOL
python3 tools/apply_dance_assignment_to_issue.py current_body.md assignment.csv > new_body.md

# 5. 用 mcp__github__issue_write(method='update', issue_number=3, body=<new_body.md 內容>) 寫回 GitHub
```

- `assign_dance_batch.py` 讀取 `kols/index.json` 目前所有 `status: active` 的 KOL 名單（目前 12 位），
  每支影片洗牌後輪流分配給不同 KOL，同一支影片只分配給一位 KOL，天然滿足「不重複」；影片數多於 KOL 數時
  盡量平均分配，並印出每位 KOL 分配到幾支供人工核對
- `parse_dance_issue.py` 只處理「待篩選」區塊、已勾選、且連結欄位是真正網址（不是「(無連結...」佔位文字）
  的項目——**勾選了但還沒找到具體連結的項目會被跳過並印警告**，不會被誤當成可分配的候選
- `apply_dance_assignment_to_issue.py` 只動「待篏選→已分配」這兩個區塊，不會動到「命名趨勢情報」區塊
  或已經存在「已分配」裡的舊項目

### 候選來源可信度（收集候選時的判斷依據，範圍不限於「這週」）

⚠️ Claude 沒有 TikTok/Instagram 的即時熱門榜 API，「這週正在紅什麼具體影片」查不到（TikTok discover
頁純前端 JS 渲染抓不到內容；一般搜尋只給得到命名趨勢情報，沒有具體連結）。**但候選片不需要限定是近期
的**——只要曾經是爆款、舞步/音樂/記憶點夠好，去年甚至更久以前的都可以用。「歷史已驗證爆款」的搜尋效果
完全不同：事後整理的文章、原創者帳號、甚至原始貼文連結都查得到，且常附帶按讚數等互動數據佐證真的紅過
（2026-08-05 實測：搜到「Say So」「Renegade」等經典舞蹈的原創者帳號與具體貼文連結，附百萬讚等級數據）。
三種來源可信度不同，寫進 issue 的「來源類型」欄位要標清楚：

- **使用者自行瀏覽時丟連結**（最可靠——使用者能看到真實演算法推播的內容，Claude 不能）
- **歷史爆款搜尋**（可信度中高——搜「XX 舞蹈 原創者/original creator」這類查詢常有具體連結+互動數據
  佐證；要註記是「已驗證有連結」還是「只知道創作者帳號/姓名，沒挖到直接連結」）
- **近期命名趨勢情報**（僅供參考選歌/選風格方向，幾乎沒有具體連結，放進「命名趨勢情報」區塊，不放進
  可核准的「待篩選」checklist）
- 不論哪種來源，人工篩選前都要**自己打開連結看過**，確認內容、日期、畫質是否真的適合當動作驅動片

> ⚠️ Method B **不適合當日常大量出片的主力**——每支片都需要人工找片、下載、裁剪，
> 音樂也被驅動片綁死換不掉（見下方「已知限制」）。當「偶爾幾支重點片」的加分手段用。

---

## 背景與場景：依「這支舞」決定，不是固定套版

> **重要**：本流程**不**規定固定的場景/打光組合（例如「一律窗光」）。每支舞的場景要依
> **這首歌的氛圍、這支驅動片的舞蹈能量、這位 KOL 的人設**三者去選，套用固定模板反而會讓
> 一批影片看起來單一、沒有吸引力。

場景/打光的選擇邏輯：

1. **先看驅動片的舞蹈能量**：俐落街舞感 → 適合鏡面教室 / 頂燈聚光；柔和律動感 → 適合居家窗光 / 臥室暖燈
2. **再看這位 KOL 的既有場景庫**：從 [`SEXY_SCENE_LIBRARY.md`](SEXY_SCENE_LIBRARY.md) 挑一個符合她人設的場景，
   不要為了配合驅動片而讓場景背離角色調性
3. **「反差感場景」是額外加分項，非必須**：精緻造型 × 意外場景（頂樓、市場、鄉間）能提升記憶點，
   但不是每支都要反差，日常合理的場景（居家、街頭、健身房）一樣可用
4. **每一批次之間刻意輪替場景**：同一位 KOL 連續幾支都用同一種場景，觀眾會覺得單調——
   這點呼應 `SEXY_SCENE_LIBRARY.md` 對靜態圖也有的「構圖多樣性」要求，影片同理適用

**固定不變的只有「讓畫面像真的」這一層**（次級動態、微表情、皮膚質感、鏡頭關係），
這層是技術規格，跟場景美術選擇是兩件事，不要混為一談。

---

## 完整流程

### Step 0：選片（人工判斷 + 記錄）

1. 找一支近期在 TikTok / Instagram 上跳得起來的舞蹈（有明確副歌記憶點動作、卡點乾淨、
   已被多人翻跳驗證過會紅——這是選片的核心判準，不是隨便一支舞都值得複製）
2. 記錄來源：帳號、貼文連結、抓取日期（熱曲會過期，記錄日期方便之後判斷是否還新鮮）
3. 確認授權狀況：這支片的音樂與舞蹈動作，是否有可商用替代方案（見 Step 5、Step 7 的授權說明）

### Step 1：下載驅動片

```bash
# IG/TikTok 公開貼文可用 yt-dlp 直接下載；私人帳號需登入 cookie
# IG 直接用 WebFetch/瀏覽器抓取會被擋 429，必須用 yt-dlp
yt-dlp -o "driver_raw.mp4" "<貼文連結>"
```

> 若執行環境沒有 `yt-dlp`，先 `pip install yt-dlp`。下載內容僅供**內部動作參考驅動**使用，
> 不得二次發佈原始素材（見 Step 7 授權說明）。

### Step 2：裁剪驅動片（決定「抖不抖」的關鍵一步）

用 ffmpeg 裁成單人中央 9:16，**裁剪寬度決定次級動態能不能保留**：

| 裁法 | 臉部佔比 | 軀幹次級動態 | 適用 |
|------|---------|------------|------|
| 腰上緊裁 | 大（~19%） | ✗ 被裁掉 | 只想要表情素材，不要這支當主 driver |
| **三分身（mid-thigh up）** | 中（~12%） | **✓ 完整保留** | **舞蹈主驅動，預設用這個** |

```bash
# 範例：裁成單人中央、三分身、去背景群舞干擾、去音軌另存
ffmpeg -i driver_raw.mp4 -vf "crop=in_w*0.5:in_h:in_w*0.25:0" -an driver_cropped.mp4

# 音樂另外抽出來，記錄裁切起點（下面 Step 5 對拍要用到）
ffmpeg -i driver_raw.mp4 -vn -acodec copy driver_audio.m4a
```

如果驅動片有多人齊舞，優先挑**前排 lead**入鏡的段落裁切，避免其他舞者的動作干擾。

> **⚠️ 2026-08-07 新增規則（Mia Huang R1 案例學到的教訓）**：**不論這支驅動片需不需要裁切畫面，都要
> 用 `ffprobe -show_entries stream=codec_name` 確認編碼是否為 H.264；不是的話一律用
> `ffmpeg -c:v libx264 -pix_fmt yuv420p` 重新編碼。** 原因：Luna Tanaka 那支驅動片因為需要裁切，
> ffmpeg 的 crop 濾鏡意外把它強制轉成了 H.264；Mia Huang 那支不需要裁切（人物已經置中），直接
> `cp` 複製保留了 Instagram 原始的 **VP9** 編碼，結果 `motion_control` 呼叫兩次都在 `in_progress`
> 後直接變成 `failed`（沒有任何錯誤訊息），排查後才發現是編碼問題，轉成 H.264 後第三次呼叫就成功。
> **不要假設 yt-dlp 從 Instagram 下載下來的檔案編碼一致**——不同貼文、不同時間下載，dash 串流給的
> 編碼格式會不一樣。這一步不能因為「這支不需要裁切」就跳過。

```bash
# 沒有裁切需求時，仍要確認/正規化編碼：
ffprobe -v error -show_entries stream=codec_name -of default=noprint_wrappers=0 driver_raw.mp4
# 如果不是 h264，重新編碼：
ffmpeg -i driver_raw.mp4 -c:v libx264 -pix_fmt yuv420p -an driver_cropped.mp4
```

### Step 3：Performance Sheet + Emotion Timeline（自動化，用兩個 director agent）

**呼叫 `performance-director` agent**，讀入 `driver_cropped.mp4` 描述（幾點幾秒做什麼動作、
服裝有沒有會飄的元素、鏡位如何），產出 Performance Sheet：段落結構、次級動態載體、鏡頭關係。

**呼叫 `emotion-director` agent**，產出 Emotion Timeline：微表情時間軸、眼神腳本、不對稱指令。

> 兩個 agent 都會要求「驅動片必選標準：選表情密度而非舞蹈品質」——如果驅動片整段掛著同一個笑容，
> 那個面具會原封不動轉印到 KOL 臉上。**上線前務必跑「停格 +5 幀比對」**：暫停在驅動片任一幀，
> 往後跳 5 幀比對，髮/衣/嘴角有變化＝可用，完全相同＝换一支驅動片。

### Step 4：生成 KOL 的角色圖（決定場景）

依上方「背景與場景」邏輯挑好場景後，用已訓練的 `soul_id` 生成一張三分身正面全身圖：

> **⚠️ 2026-08-06 更正**：本步驟原本預設 `count=2`（生成兩張候選再挑一張），但起始畫面只有一張會
> 真正被拿去跑 Motion Control，另一張等於白花——**改為預設 `count=1`**，把 prompt 一次寫到位（場景、
> 服裝、光線都在上方「背景與場景」邏輯裡先決定好），不要用「生兩張再選」取代想清楚 prompt。
> 只有在使用者明確要求比較多個構圖/角度時，才臨時提高 count。

```python
generate_image(
    model="soul_2",
    soul_id="<KOL_SOUL_ID>",
    prompt="[身材描述], [髮型描述], "
           "standing in confident pose facing camera, "
           "wearing [服裝 — 必須含至少一件會動的元素：薰紗外套/罩衫/長髮放下/耳環], "
           "THREE QUARTER SHOT, mid-thigh up, no shoes shown, "
           "[本次選定場景描述], [本次選定打光描述], "
           "film grain, candid lifestyle photo, warm tones, shot on 35mm",
    aspect_ratio="9:16",
    count=1
)
```

> **⚠️ 2026-08-12 新增（先查該 KOL 有沒有專屬 prompt 格式，不要無腦套用上面的通用範本）**：上面這段
> 是**通用範本**，但部分 KOL（例如 Rainie Hsu，見 `kols/rainie-hsu/generation_notes.md` 開頭「核心
> Prompt 結構」章節）已經訂過自己專屬的 prompt 措辭規則，會明確禁止某些字眼。Rainie 案例的實測教訓：
> 通用範本裡的 `film grain`／`shot on 35mm`／`candid lifestyle photo` 這組詞，對她的 soul_id 連續
> 3 次觸發「拼貼／三連版面」的生成bug（一度誤判是 soul_id 本身壞掉，差點要花 credit 重新訓練）——
> 換成她專屬格式裡規定的 `crisp sharp focus`、`high dynamic range`、`high-production-value`、
> `Instagram style` 後，一次就生成乾淨單張圖。**做 Step 4 之前，先看該 KOL 的 `generation_notes.md`
> 開頭是否有「核心 Prompt 結構」或類似章節，有就套用那個格式，上面的通用範本只在沒有專屬格式時使用。**

> **⚠️ 人工核准關卡（見文件開頭「怎麼觸發」補充）**：生成後**停在這裡，把圖給使用者看**，等使用者
> 核准或指示重生成，**核准之後才進 Step 5**。不要自動接著跑 Motion Control。

服裝規則沿用 `DANCE_VIDEO_SOP.md` 既有的服裝避雷清單（不能用露腹/belly dance bra 等會被像素過濾的服裝），
但**新增一條**：至少一件會動的元素（寬版薰紗外套/罩衫/長髮/圈耳環），這是次級動態的載體，缺了它
performance-director 應該判定為 blocking issue、要求换裝重生成。

### Step 5：Motion Control 生成

```python
motion_control(
    model="kling3_0",  # Motion Control / Kling 3.0
    reference_image={"id": "<KOL角色圖 image_media_id>"},
    driver_video={"id": "<driver_cropped.mp4 上傳後的 media_id>"},
    scene_control="image",   # 保留 KOL 自己的場景（步驟4選定的背景），借用 driver 的動作與表情
    # scene_control="video" 則是反過來：借用 driver 影片本身的背景/打光，KOL 只是「被套進那個場景」
    #   — 需要驅動片的場景剛好適合這位 KOL 時才用這個選項，預設用 image
    resolution="1080p",
    aspect_ratio="9:16"
)
```

> **⚠️ 2026-08-05 更正輸出提醒**：本文件原本寫「輸出不含音軌」，這是錯的假設，未經驗證直接沿用。
> 實測（Coco Wu 案例）發現：**當 `motion_video_id` 的驅動片本身含音軌時，`motion_control` 會自動輸出
> 已經合成好、且跟生成動作對時過的音樂**（不是原始檔案的位元複製，是重新合成/retiming 過的版本，
> 位元率通常比驅動片原始音軌更高、時長精確貼合生成畫面長度）。**先用 `ffprobe` 檢查 Step 5 輸出檔案
> 是否已有音軌，再決定要不要跑 Step 6**——多數情況下輸出已經是「畫面+音樂」合一的成品，
> 不需要再手動混音。手動混音（Step 6）現在只當作「輸出真的無聲」或「要換成別的授權配樂」時的備用手段，
> 不是預設必經步驟。

`scene_control` 怎麼選：

| 選項 | 效果 | 何時用 |
|------|------|--------|
| `image`（預設） | KOL 場景 + driver 動作/表情 | 大多數情況——保留角色一致性與尺度控制 |
| `video` | driver 場景/打光 + KOL 換上去 | driver 的場景本身很適合這位 KOL 調性時（例：鏡面舞蹈教室配活力系角色），但要注意背景可能殘留 driver 的浮水印/招牌 |

> **⚠️ 2026-08-08 發現（鏡子是背景動態的例外，不是通則）**：`scene_control: "image"` 模式下，背景
> 整體是鎖死的靜態參考圖，這件事本身沒有變——但 R10（Rainie Hsu）成品事後比對發現，起始畫面裡的化妝鏡
> 反射區域，內容會隨著本人動作變化（例如舉手時鏡中對應輪廓也跟著變化），不是完全靜止的貼圖。判斷原因：
> 模型訓練資料裡「鏡子自拍」影片的鏡中影像永遠跟著鏡子前的人同步變動，這個關聯性極強，強到能局部突破
> `scene_control: "image"` 把背景整體鎖死的預設行為；但風吹樹動、車流、行人這類環境動態沒有這種物理鐵律
> 等級的訓練關聯，不會被同樣的機制帶出來，`scene_control: "image"` 對這類背景依然是完全靜止的。
> **可操作的啟示**：之後設計起始畫面場景時，若想讓背景多一點「活」的感覺又不想冒 `scene_control: "video"`
> 的審核風險，可以刻意在構圖裡安排鏡子/玻璃/光滑反光地面，換取局部背景動態；但不要預期戶外街景的樹木/
> 車流會用這個方法動起來，那類環境動態目前仍只能靠 `scene_control: "video"`（有審核風險）或後製運鏡
> 手法（如 R6/R7 的 `ffmpeg` 漂移平移，模擬手持感，不會讓背景本身的物件動起來）。

> **⚠️ 2026-08-08 發現（Motion Control 的身分覆蓋風險——起始畫面髮型輪廓需貼近驅動片）**：R12（Coco Wu）
> 成品經使用者目視發現「臉完全變不一樣」——把 v1 成品幀跟已核准的 v1 起始畫面並排比對，確認臉型/五官/
> 髮型全部跑掉，且跑掉後的長相跟**驅動片本人**（雙馬尾+圓臉大眼）幾乎一致。**根因**：Motion Control
> 的臉部/髮型鎖定完全依賴 `image_id` 這個參考錨點，沒有像 `soul_id` 那樣的強制鎖定機制（`soul_id`
> 只對 `soul_2` 靜態圖片生成有效）；當起始畫面的髮型輪廓（本例是單馬尾+散髮）跟驅動片實際需要合成的
> 動態（本例是雙馬尾大幅甩動）差異夠大時，模型可能在需要生成新髮型動態的畫面時，退回去借用驅動片本人
> 的臉部/髮型頂替，而非鎖住起始畫面的身分。**額外發現**：v1 起始畫面的原始 prompt 其實已經寫了雙馬尾，
> 但 Soul V2 實際渲染出來的是不同髮型，prompt 與輸出的落差在 Step 4 核准與 Step 8 QA 兩關都沒被抓到——
> **Step 8 QA 一律要用 Read 工具把輸出幀跟起始畫面「圖檔」本身並排比對，不能依賴 Step 3/4 筆記裡對外觀
> 的文字描述**。**修復**：重新生成起始畫面時把髮型 prompt 改寫得更明確、確認渲染結果真的貼近驅動片的
> 髮型輪廓，用同一支驅動片重跑 Motion Control 後身分穩定。**繞路嘗試與否決**：曾評估改用 Seedance 2.0
> Mini（支援 `image_references`+`video_references` 雙輸入，理論上身分鎖定機制比 Motion Control 更獨立）
> 取代 Motion Control；`get_cost` preflight 顯示完整規格下 Seedance 2.0 Mini（37.5 credit）比 Motion
> Control（49 credit）便宜，但 5 秒短版測試（12.5 credit，已退款零成本）判定 `nsfw` 失敗，推測其對
> 比基尼類服裝的審核門檻比 Motion Control 嚴格，在本 studio 以性感風格為主的內容產線上可能是結構性
> 障礙，暫緩深入測試、改回起始畫面修復路徑。**可操作的啟示**：Step 4 起始畫面設計時，若驅動片本人有
> 明顯特徵髮型（雙馬尾、包頭等），起始畫面的髮型輪廓應盡量貼近，降低 Motion Control 在動態合成時
> 「借用」驅動片本人特徵的風險；不能保證這個機制的邊界在哪，但輪廓差異越大、風險越高。
>
> **⚠️ 2026-08-08 補充發現（風險是機率性的，不是必然發生——R13–R18 批次驗證）**：R12 事故後，對 R13–R18
> 批次逐支做 Step 3 髮型輪廓核對，發現 5/6 支（R14/R15/R16/R17/R18）都有跟 R12 同一類「起始畫面髮型
> 跟驅動片本人輪廓不匹配」的風險（長短髮/直捲髮/局部造型差異程度不一）。使用者裁決不預先修正、直接
> 跑 Motion Control，結果 **5 支風險全部沒有實際發生**，Step 8 逐支圖檔並排比對確認身分保住。**結論**：
> 髮型輪廓不匹配只是升高身分覆蓋的機率，不是必然觸發——R12 的失敗可能還牽涉到其他放大因子（例如
> 該次驅動片是全片最長 18.67s、13s 處有大幅度頭部快速甩動的動態模糊窗口，動態幅度/時長可能都是共同
> 因子）。**操作上的啟示**：仍建議 Step 4 盡量讓起始畫面髮型貼近驅動片以降低風險，但**不需要因為偵測到
> 風險就自動判定必須重新生成起始畫面**——可以視風險程度、驅動片動態幅度、以及使用者對這批素材的成本
> 容忍度，選擇「先低成本修正起始畫面」或「直接跑、Step 8 加強針對性 QA」兩條路徑，兩者都是合理選項。
>
> **⚠️ 2026-08-08 發現（soul_id 的「拼貼」模型慣性——診斷與正確修法）**：Luna Tanaka 的 `soul_id` 在
> Step 4 起始畫面生成時，對特定 prompt（後來發現其實是廣泛性、不限特定 prompt）穩定產出「三連拼貼」
> 版面（一張圖裡塞 2–4 個相似分鏡縮圖），不是單張照片，無法當 Motion Control 輸入。**診斷方式**：
> 換多種措辭/長寬比/`count` 抽樣皆 100% 拼貼 → 排除是 prompt 用詞或機率問題 → 判定是這個 soul_id 訓練
> 資料本身混入雜誌型錄式多格照片、已經寫進角色嵌入的模型慣性，且範圍是全面性的（連日常街景/咖啡廳這類
> 內容也會拼貼），不是只有特定風格描述才觸發。**錯誤的修法**：(a) 用這個已知壞掉的 soul_id 生成「新」
> 訓練圖素材，等於死循環，生出來的訓練圖也是拼貼；(b) 為了繞開問題改用不經過 soul_id 的替代模型
> （如 `nano_banana_pro` + 參考圖），雖然能避開拼貼、零訓練成本，但會犧牲身分鎖定精確度與畫面真實感
> （背景質感容易讀出 AI 感），**這兩項是本 studio 素材的核心價值，不能為了省成本妥協**。**正確修法**：
> 用 repo 裡既有、該 soul_id 訓練時期已核准的舊照片（不生成任何新素材）直接送 `show_characters
> (action='train')` 重新訓練一個新 soul_id，成本約 25 credits（訓練呼叫本身，因為用免費的既有素材，
> 不需要額外的訓練圖生成費用），訓練完成後用同樣 prompt 測試確認拼貼問題消失、身分一致性維持良好。
> 訓練耗時可能明顯久於其他 KOL 過去記錄的約 10 分鐘（此次約 35–40 分鐘），需要有耐心等待，不要在
> `in_progress` 階段就判斷失敗重試。

### Step 6：（僅當 Step 5 輸出真的無聲，或要換授權配樂時才需要）抽音樂 + 混音

> ⚠️ **先檢查 Step 5 的輸出檔案有沒有音軌**（`ffprobe -select_streams a`），多數情況下
> `motion_control` 已經自動輸出對時好的音樂，這一步可以整個跳過，直接把 Step 5 的檔案存進
> `kols/<koId>/videos/<scene>/` 當成品。以下混音流程只在兩種情況才需要：(a) 輸出真的無聲，
> 或 (b) 要把驅動片配樂換成已授權的曲庫版本。

> ⚠️ **舞步的節奏是從驅動片的音樂長出來的**——AI 生成的動作 hit 點是照著驅動片
> 原本的節拍走的，換一首完全不同的歌，卡點會全部打散，即使長度一樣也不會對。

**內部驗證/測試（輸出真的無聲時的備用路徑）**：用 Step 1 已經下載好、Step 2 已經抽出來的驅動片原始音樂
（`driver_audio.m4a`），音軌起點對齊 Step 2 的裁切起點（例如裁切從原片 0.3s 開始，音樂也從
0.3s 取），然後跟 Step 5 生成的無聲影片混音成一個成品檔——**注意這是用未經對時處理的原始音軌
硬接上去，音樂節奏對位精準度不會比 `motion_control` 自己輸出的原生版本好**，只是在原生輸出真的
無聲時的救急做法：

```bash
# 1. 音軌起點對齊裁切點，長度對齊生成影片長度
ffmpeg -i driver_audio.m4a -ss <裁切起點> -t <生成影片長度> -c copy audio_synced.m4a

# 2. 混音：把 Step 5 的無聲影片 + 對齊好的音樂合成一個檔案（這就是最終成品）
ffmpeg -i kling_output_silent.mp4 -i audio_synced.m4a \
  -c:v copy -c:a aac -map 0:v:0 -map 1:a:0 -shortest \
  kols/<koId>/videos/<scene>/final_output.mp4
```

到這一步，成品已經是「畫面 + 音樂」合一、可以直接看的檔案——**這兩個 ffmpeg 指令都由 Claude
在同一個 session 內用 Bash 直接跑完**，使用者不需要另外開剪輯軟體匯入音軌。

**要對外發佈（驅動片原曲通常沒有商用授權）**：上面的混音指令一樣適用，只是把 `audio_synced.m4a`
換成下列兩種來源之一：

1. 找同一首歌的可商用版本/授權（如果那首歌本身就是熱曲，可能有官方授權曲庫版本），直接替換即可
2. 換一首 **BPM 相近**的可商用曲，**重新抓 beat grid 對拍**（不能直接蓋上去，卡點會不對）——
   這比選項 1 麻煩，但換歌後才能保留「當週熱曲」的靈活性：

```bash
# 用 ffmpeg atempo 把新歌的速度微調到跟驅動片節奏一致（BPM 落差不大時可用；
# BPM 差太多要整首重新抓拍，不能只靠調速）
ffmpeg -i new_song.mp3 -filter:a "atempo=<比例>" -ss <裁切起點> -t <影片長度> new_song_synced.mp3
# 再用上面的混音指令 2，把 new_song_synced.mp3 當音軌來源合成成品
```

### Step 7：授權與發佈限制檢查（發片前必查）

| 項目 | 檢查 |
|------|------|
| 驅動動作 | 來自第三方創作者，**動作參考驅動用於內部方法驗證合理，但生成結果若明顯可辨識出是複製誰的編舞，發佈前要考慮是否需要致敬標註或改編到不可辨識程度** |
| 配樂 | **✅ 2026-08-25 更正：Challenge 熱門音訊可直接沿用，不需替換。**見下方說明 |
| 背景 | 若用 `scene_control="video"` 借用了驅動片場景，檢查背景是否殘留驅動片來源的浮水印/招牌/可辨識地標，發佈前需去除或裁掉 |
| 素材存放 | 驅動片原始檔（`driver_raw.mp4`／`driver_cropped.mp4`）與其原始音軌**只存在本機工作資料夾，不進 git**，避免誤上傳到 repo（`videos/` 底下最終只放生成完成的成品） |

> **⚠️ 2026-08-25 配樂規則更正（Penny 裁示）**
>
> 本 SOP 先前要求「正式發佈前必須把驅動片原曲換成可商用曲庫版本」。
> **這個要求對 Challenge 類熱門音訊不成立，已撤銷。**
>
> 原因：這些音樂本來就是社群平台上供大家做 Challenge 的熱門音訊，
> **發布後平台會自動辨識音樂並帶上版權資訊**；驅動片本身也是別人用同一首歌
> 發的 Challenge 影片——跟著做 Challenge 反而是在幫音樂宣傳，那正是這類音訊存在的目的。
>
> → **沿用驅動片原曲直接發布即可，不需重新對拍。**
>
> 仍需替換的情況只剩兩種：(a) 該曲並非平台上的公開 Challenge 音訊，
> 而是有明確商用限制的一般錄音；(b) 客戶／品牌合約另有指定配樂。
>
> **注意**：各 KOL `generation_notes.md` 裡 2026-08-25 之前的 Step 7 記錄，
> 仍寫著舊的「必須替換配樂」結論。那些是當時的判斷紀錄，不再適用，以本節為準。

### Step 8：品質檢核（AI 感偵測，接 QA workflow）

生成完成後，跑以下檢查（已寫入 `.claude/workflows/kol_content_qa_pipeline.js` 的 Quality Critique 階段）：

- [ ] **次級動態**：暫停在身體停止的瞬間 → 髮/衣是否仍在動？（同步靜止 = 失敗）
- [ ] **微表情**：抽 3 個相隔 1 秒的幀 → 表情是否都不一樣？（相同 = 面具感）
- [ ] **不對稱**：臉是否過度對稱？頭是否始終正對鏡頭？（是 = 失敗）
- [ ] **皮膚質感**：放大臉部 → 是否有毛孔與高光層次？（全平滑 = 蠟像，且**舞蹈片不上美顏濾鏡**——
      磨皮會連布料/髮絲一起糊掉，削弱次級動態的可讀性，臉變美但身體變鈍）
- [ ] **鏡頭關係**：眼神是否有焦點且與鏡頭互動？
- [ ] **手部**：手指數量、形狀正確
- [ ] **卡拍**：動作關鍵幀是否落在音樂重拍上（Step 6 對拍是否確實對齊）
- [ ] **規格**：1080×1920 以上、30fps 以上、音樂已對齊長度

發片前 rubric 沿用四軸判準（性感/青春/娛樂/優雅），見 `SEXY_SCENE_LIBRARY.md` 與各 KOL `content_style.md`。

---

## 已知限制（誠實列出，不要假裝這套方法沒有代價）

| 限制 | 說明 |
|------|------|
| **選片仍需人工判斷** | Step 0 挑哪支舞值得複製是人的審美判斷（有沒有記憶點、卡不卡拍），這一步無法自動化；但選好之後 Step 1–6（下載、裁剪、生成、混音）都是 Claude 一句指令內執行完成，不是逐步手動操作 |
| **音樂不自由** | 換歌等於打散卡點，犧牲了 Method A「隨時换當週熱曲」的優勢 |
| **驅動片版權** | 動作與音樂都來自第三方創作者，正式發佈前必須完成 Step 7 的授權檢查 |
| **IG 抓取限制** | 公開 Reel 可用 `yt-dlp`，私人帳號需登入 cookie；直接用瀏覽器/WebFetch 常被 429 擋下 |
| **依賴外部工具** | 需要 `yt-dlp` + `ffmpeg` 在執行環境中可用 |

---

*建立日期：2026-08-05*
*方法來源：改編自 `firekou/Buildup_KOL` 的動作驅動法（`docs/04`–`07`），依本 studio 的場景庫與已驗證失敗案例調整；場景/背景刻意改為依歌曲/人設彈性選擇，不採用其固定模板。*
